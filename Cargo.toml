[package]
name = "aur-gate"
version = "0.1.0"
edition = "2021"
rust-version = "1.86"
description = "Deterministic pre-install gate for Arch Linux AUR updates"
license = "MIT"
repository = "https://github.com/bermudi/aur-gate"

[dependencies]
regex = "1"
anyhow = "1"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
tempfile = "3"
llm = { version = "1.3.8", default-features = false, features = ["rustls-tls", "openai", "anthropic", "ollama", "deepseek", "openrouter"] }
tokio = { version = "1", features = ["rt-multi-thread"] }
nix = { version = "0.29", default-features = false, features = ["fs"] }

[[test]]
name = "missing_cache_http"
harness = false

[[test]]
name = "wrapper_transaction"
harness = false

[[test]]
name = "command_flow"
harness = false

[profile.release]
lto = true
